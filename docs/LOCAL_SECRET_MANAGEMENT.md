# Local secret management

Real credentials must remain in Git-ignored local files.

- WeChat and WeChat Pay secrets:
  `~/.seafood-secrets/seafood-commerce/`
- Local administrator credentials:
  `.local/env/admin.local`
- Database settings:
  `.env`

The public repository stores examples and loader scripts only.
